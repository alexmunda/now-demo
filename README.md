# now-demo
Install now
```
npm install now -g
```

## now-serve
Build the project
```
npm install

elm-package install

npm run build
```

Deploy with now-serve
```
cd dist
now-serve
```

## now
```
git checkout microservice

now
```
